import colorVars from "@common/colors.module.scss";

const colors = {
  primaryColor: colorVars.primaryColor,
  primaryContrastColor: colorVars.primaryContrastColor,
  secondaryColor: colorVars.secondaryColor,
  lightPaperColor: colorVars.lightPaperColor,
  lightTableBackgroundColor: colorVars.lightTableBackgroundColor,
  lightFooterBackgroundColor: colorVars.lightFooterBackgroundColor,
  lightBackgroundColor: colorVars.lightBackgroundColor,
  lightTextColor: colorVars.lightTextColor,
  darkPaperColor: colorVars.darkPaperColor,
  darkBackgroundColor: colorVars.darkBackgroundColor,
  darkFooterBackgroundColor: colorVars.darkFooterBackgroundColor,
  headingColor: colorVars.headingColor,
};

export default colors;
